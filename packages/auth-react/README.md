# auth-react

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build auth-react` to build the library.

## Running unit tests

Run `nx test auth-react` to execute the unit tests via [Vitest](https://vitest.dev/).
