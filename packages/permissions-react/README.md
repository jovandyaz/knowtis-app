# permissions-react

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build permissions-react` to build the library.

## Running unit tests

Run `nx test permissions-react` to execute the unit tests via [Vitest](https://vitest.dev/).
