# permissions-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build permissions-core` to build the library.

## Running unit tests

Run `nx test permissions-core` to execute the unit tests via [Vitest](https://vitest.dev/).
