# auth-nestjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build auth-nestjs` to build the library.

## Running unit tests

Run `nx test auth-nestjs` to execute the unit tests via [Vitest](https://vitest.dev/).
