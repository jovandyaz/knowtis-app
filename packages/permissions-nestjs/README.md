# permissions-nestjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build permissions-nestjs` to build the library.

## Running unit tests

Run `nx test permissions-nestjs` to execute the unit tests via [Vitest](https://vitest.dev/).
