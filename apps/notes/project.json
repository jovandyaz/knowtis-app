{
  "name": "notes",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/notes/src",
  "projectType": "application",
  "tags": ["type:app", "scope:notes"],
  "implicitDependencies": [
    "design-system",
    "data-access-notes",
    "editor-schema",
    "shared-hooks",
    "shared-util",
    "crdt"
  ],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "inputs": [
        "production",
        "^production",
        { "env": "VITE_API_URL" },
        { "env": "VITE_WS_URL" },
        { "env": "VITE_COLLABORATION_MODE" },
        { "env": "VITE_MCP_URL" },
        { "env": "VITE_PUBLIC_POSTHOG_KEY" },
        { "env": "VITE_PUBLIC_POSTHOG_HOST" },
        { "env": "VERCEL_GIT_COMMIT_SHA" }
      ],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/notes"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "dependsOn": ["^build"],
      "options": {
        "buildTarget": "notes:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "notes:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "notes:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "notes:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "notes:build:development"
        },
        "production": {
          "buildTarget": "notes:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "reportsDirectory": "../../coverage/apps/notes",
        "passWithNoTests": true,
        "configFile": "apps/notes/vitest.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools/typecheck-project.sh {projectRoot}"
      }
    }
  }
}
