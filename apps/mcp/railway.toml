# Railway Configuration for Knowtis MCP Server (knowtis-mcp service)
# https://docs.railway.com/guides/config-as-code

[build]
# Build command for MCP server in Nx monorepo
builder = "nixpacks"
buildCommand = "NODE_ENV=development pnpm install --frozen-lockfile && pnpm nx build mcp"
# No watchPatterns: they make `railway up` skip the build when no watched file
# changed, silently deploying nothing while CI reports success. CI already
# gates deploys with nx affected — when the deploy job runs, Railway must build.

[deploy]
startCommand = "node dist/apps/mcp/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Railway config-as-code has no [env]/[service] sections — env vars cannot be
# set from this file. Service variables (NODE_ENV, MCP_ALLOWED_HOSTS, ...) are
# pinned by the deploy-mcp job in .github/workflows/ci.yml before `railway up`.
